---
type: setup-guide
tags: [workflow/web-clipper, system/inbox]
---

# Web Clipper Setup

模板文件：`98-AI-Context/Automation/Research-Capture.json`。

在 Obsidian Web Clipper 扩展中打开 Templates，导入该 JSON，并选择当前 Vault。模板会把标题、来源、作者、发布时间、抓取时间和正文保存到 `00-Inbox/Downloaded`。模板不调用 AI，不总结、不删减、不改写观点。
