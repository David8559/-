---
type: generated-code-shard
status: generated
topic_hub: 优化模型 Hub
algorithm: 整数规划
tags: [area/数学建模, workflow/源码索引, status/待运行验证]
---

<!-- GENERATED-CODE-SHARD: DO NOT EDIT BY HAND -->

# 整数规划 · 原始源码实现

> [!warning] 使用边界
> 本页由本地目录自动生成，保留源码、SHA-256 与原始路径。静态检查不等于运行正确；正式调用前必须补齐依赖、最小样例和结果检验。

- 领域入口：[[04-Research/02-经典建模模型库/03-优化类模型/优化模型 Hub|优化模型 Hub]]
- 独立实现：6
- 原始来源：6
- 逐文件状态：[[04-Research/03-建模算法源码库/代码验证报告|代码验证报告]]

决策变量含整数或0-1约束，用于选择、分配和组合优化。

#### ex2_6 · LINGO · fc6a8327

- 归属算法：整数规划
- 用途：核心算法与辅助函数
- 独立实现变体：1
- 原始来源文件：1
- 复习入口：[[04-Research/02-经典建模模型库/03-优化类模型/优化模型 Hub#整数规划 · 复习|整数规划 · 复习]]
- 验证状态：`source-traced`；尚未运行；自动归类不代表算法或结果正确。

##### 代码理解与调用

- **定位**：函数/类型实现，可优先封装复用；当前用于整数规划中的“核心算法与辅助函数”。
- **执行主线**：声明整数/0-1变量并求解离散优化模型；在LINGO中声明集合、目标函数和约束后交给求解器。
- **调用方式**：优先调用 `data`、`model`、`sets`；先核对参数顺序和返回值。
- **主要输出**：工作区变量或求解器结果
- **调用风险**：未发现明显静态风险，但仍需运行测试和结果校验。

- SHA-256：`2601ce344df1aefa6b9825aee2f7e1cc3ad6bea8b4bcca5ceb737ce93637355b`
- 语言：LINGO
- 符号：`data`, `model`, `sets`
- 原始路径：
  - 源文件：`00-Inbox/Downloaded/6.数学建模模型算法大全/司守奎数学建模算法课件/程序及数据/02第2章/ex2_6.lg4`

<details>
<summary>展开原始代码</summary>

````text
model:

sets:

row/1..4/:b;

col/1..5/:c1,c2,x;

link(row,col):a;

endsets

data:

c1=1,1,3,4,2;

c2=-8,-2,-3,-1,-2;

a=1 1 1 1 1

  1 2 2 1 6

  2 1 6 0 0

  0 0 1 1 5;

b=400,800,200,200;

enddata

max=@sum(col:c1*x^2+c2*x);

@for(row(i):@sum(col(j):a(i,j)*x(j))<b(i));

@for(col:@gin(x));

@for(col:@bnd(0,x,99));

end
````

</details>

#### ex2_7 · LINGO · bb48a65b

- 归属算法：整数规划
- 用途：核心算法与辅助函数
- 独立实现变体：1
- 原始来源文件：1
- 复习入口：[[04-Research/02-经典建模模型库/03-优化类模型/优化模型 Hub#整数规划 · 复习|整数规划 · 复习]]
- 验证状态：`source-traced`；尚未运行；自动归类不代表算法或结果正确。

##### 代码理解与调用

- **定位**：函数/类型实现，可优先封装复用；当前用于整数规划中的“核心算法与辅助函数”。
- **执行主线**：声明整数/0-1变量并求解离散优化模型；在LINGO中声明集合、目标函数和约束后交给求解器。
- **调用方式**：优先调用 `data`、`model`、`sets`；先核对参数顺序和返回值。
- **主要输出**：工作区变量或求解器结果
- **调用风险**：未发现明显静态风险，但仍需运行测试和结果校验。

- SHA-256：`112ad1c8302040fce0c3cb2cb3a312964fb90a1cab4755ed5b6852fa3d68841e`
- 语言：LINGO
- 符号：`data`, `model`, `sets`
- 原始路径：
  - 源文件：`00-Inbox/Downloaded/6.数学建模模型算法大全/司守奎数学建模算法课件/程序及数据/02第2章/ex2_7.lg4`

<details>
<summary>展开原始代码</summary>

````text
model:

sets:

var/1..5/;

link(var,var):c,x;

endsets

data:

c=3 8 2 10 3

  8 7 2 9 7

  6 4 2 7 5

  8 4 2 3 5

  9 10 6 9 10;

enddata

min=@sum(link:c*x);

@for(var(i):@sum(var(j):x(i,j))=1);

@for(var(j):@sum(var(i):x(i,j))=1);

@for(link:@bin(x));

end
````

</details>

#### ex2_7 · MATLAB · 8104de95

- 归属算法：整数规划
- 用途：核心算法与辅助函数
- 独立实现变体：1
- 原始来源文件：1
- 复习入口：[[04-Research/02-经典建模模型库/03-优化类模型/优化模型 Hub#整数规划 · 复习|整数规划 · 复习]]
- 验证状态：`source-traced`；尚未运行；自动归类不代表算法或结果正确。

##### 代码理解与调用

- **定位**：脚本型示例，适合学习后重构；当前用于整数规划中的“核心算法与辅助函数”。
- **执行主线**：按脚本或函数顺序完成数值计算并返回工作区结果。
- **调用方式**：不要直接复制整段到项目；先把硬编码参数移到函数入口，再用最小样例调用。
- **主要输出**：工作区变量或求解器结果
- **调用风险**：未发现明显静态风险，但仍需运行测试和结果校验。

- SHA-256：`83f6830b4b9378f532a628fa8f8c34c87623bf68bc878d4b291a815dfdac056f`
- 语言：MATLAB
- 符号：未自动识别
- 原始路径：
  - 源文件：`00-Inbox/Downloaded/6.数学建模模型算法大全/司守奎数学建模算法课件/程序及数据/02第2章/ex2_7.m`

<details>
<summary>展开原始代码</summary>

````matlab
c=[3 8 2 10 3;8 7 2 9 7;6 4 2 7 5
   8 4 2 3 5;9 10 6 9 10];
c=c(:);
a=zeros(10,25);
for i=1:5
   a(i,(i-1)*5+1:5*i)=1;
   a(5+i,i:5:25)=1;
end
b=ones(10,1);
[x,fval]=bintprog(c,[],[],a,b);
x=reshape(x,[5,5]), fval
````

</details>

#### ex4_15 · LINGO · 2e96d690

- 归属算法：整数规划
- 用途：核心算法与辅助函数
- 独立实现变体：1
- 原始来源文件：1
- 复习入口：[[04-Research/02-经典建模模型库/03-优化类模型/优化模型 Hub#整数规划 · 复习|整数规划 · 复习]]
- 验证状态：`source-traced`；尚未运行；自动归类不代表算法或结果正确。

##### 代码理解与调用

- **定位**：函数/类型实现，可优先封装复用；当前用于整数规划中的“核心算法与辅助函数”。
- **执行主线**：声明整数/0-1变量并求解离散优化模型；在LINGO中声明集合、目标函数和约束后交给求解器。
- **调用方式**：优先调用 `DATA`、`MODEL`、`SETS`、`model`；先核对参数顺序和返回值。
- **主要输出**：工作区变量或求解器结果
- **调用风险**：未发现明显静态风险，但仍需运行测试和结果校验。

- SHA-256：`5cb16fa4c77290232fde2c9e16f896e9aced6cd8ac50f50acb4e531ab11ad10c`
- 语言：LINGO
- 符号：`DATA`, `MODEL`, `SETS`, `model`
- 原始路径：
  - 源文件：`00-Inbox/Downloaded/6.数学建模模型算法大全/司守奎数学建模算法课件/程序及数据/04第4章/ex4_15.lg4`

<details>
<summary>展开原始代码</summary>

````text
MODEL:

  SETS:

  CITY / 1.. 10/: U; ! U( I) = sequence no. of city;

  LINK( CITY, CITY):

       DIST,  ! The distance matrix;

          X;  ! X( I, J) = 1 if we use link I, J;

 ENDSETS

 DATA:   !Distance matrix, it need not be symmetric;

  DIST =0     8     5     9    12    14    12    16    17    22

     8     0     9    15    17     8    11    18    14    22

     5     9     0     7     9    11     7    12    12    17

     9    15     7     0     3    17    10     7    15    18

    12    17     9     3     0     8    10     6    15    15

    14     8    11    17     8     0     9    14     8    16

    12    11     7    10    10     9     0     8     6    11

    16    18    12     7     6    14     8     0    11    11

    17    14    12    15    15     8     6    11     0    10

    22    22    17    18    15    16    11    11    10     0;

 ENDDATA

 !The model:Ref. Desrochers & Laporte, OR Letters,  Feb. 91;

  N = @SIZE( CITY);

  MIN = @SUM( LINK: DIST * X);

  @FOR( CITY( K):

  !  It must be entered;

   @SUM( CITY( I)| I #NE# K: X( I, K)) = 1;

  !  It must be departed;

   @SUM( CITY( J)| J #NE# K: X( K, J)) = 1;

  ! Weak form of the subtour breaking constraints;

  ! These are not very powerful for large problems;

   @FOR( CITY( J)| J #GT# 1 #AND# J #NE# K:

       U( J) >= U( K) + X ( K, J) -

       ( N - 2) * ( 1 - X( K, J)) +

       ( N - 3) * X( J, K)));

  ! Make the X's 0/1;

  @FOR( LINK: @BIN( X));

  ! For the first and last stop we know...;

  @FOR( CITY( K)| K #GT# 1:

   U( K) <= N - 1 - ( N - 2) * X( 1, K);

   U( K) >= 1  + ( N - 2) * X( K, 1));

END
````

</details>

#### ex4_3 · LINGO · cf195ec4

- 归属算法：整数规划
- 用途：核心算法与辅助函数
- 独立实现变体：1
- 原始来源文件：1
- 复习入口：[[04-Research/02-经典建模模型库/03-优化类模型/优化模型 Hub#整数规划 · 复习|整数规划 · 复习]]
- 验证状态：`source-traced`；尚未运行；自动归类不代表算法或结果正确。

##### 代码理解与调用

- **定位**：函数/类型实现，可优先封装复用；当前用于整数规划中的“核心算法与辅助函数”。
- **执行主线**：声明整数/0-1变量并求解离散优化模型；在LINGO中声明集合、目标函数和约束后交给求解器。
- **调用方式**：优先调用 `data`、`model`、`sets`；先核对参数顺序和返回值。
- **主要输出**：工作区变量或求解器结果
- **调用风险**：未发现明显静态风险，但仍需运行测试和结果校验。

- SHA-256：`f56185e660066995b948b5e175ae46e3e5710ae8afe07195558d031de192f09d`
- 语言：LINGO
- 符号：`data`, `model`, `sets`
- 原始路径：
  - 源文件：`00-Inbox/Downloaded/6.数学建模模型算法大全/司守奎数学建模算法课件/程序及数据/04第4章/ex4_3.lg4`

<details>
<summary>展开原始代码</summary>

````text
model:

sets:

cities/1..11/;

roads(cities,cities):w,x;

endsets

data:

w=0;

enddata

calc:

w(1,2)=2;w(1,3)=8;w(1,4)=1;

w(2,3)=6;w(2,5)=1;

w(3,4)=7;w(3,5)=5;w(3,6)=1;w(3,7)=2;

w(4,7)=9;

w(5,6)=3;w(5,8)=2;w(5,9)=9;

w(6,7)=4;w(6,9)=6;

w(7,9)=3;w(7,10)=1;

w(8,9)=7;w(8,11)=9;

w(9,10)=1;w(9,11)=2;w(10,11)=4;

@for(roads(i,j):w(i,j)=w(i,j)+w(j,i));

@for(roads(i,j):w(i,j)=@if(w(i,j) #eq# 0, 1000,w(i,j)));

endcalc

n=@size(cities);  !城市的个数;

min=@sum(roads:w*x);

@for(cities(i)|i #ne#1 #and# i #ne# n:@sum(cities(j):x(i,j))=@sum(cities(j):x(j,i)));

@sum(cities(j):x(1,j))=1;

@sum(cities(j):x(j,1))=0; !不能回到顶点1;

@sum(cities(j):x(j,n))=1;

@for(roads:@bin(x));

       end
````

</details>

#### ex7_3_2 · LINGO · a5775e93

- 归属算法：整数规划
- 用途：核心算法与辅助函数
- 独立实现变体：1
- 原始来源文件：1
- 复习入口：[[04-Research/02-经典建模模型库/03-优化类模型/优化模型 Hub#整数规划 · 复习|整数规划 · 复习]]
- 验证状态：`source-traced`；尚未运行；自动归类不代表算法或结果正确。

##### 代码理解与调用

- **定位**：函数/类型实现，可优先封装复用；当前用于整数规划中的“核心算法与辅助函数”。
- **执行主线**：声明整数/0-1变量并求解离散优化模型；在LINGO中声明集合、目标函数和约束后交给求解器。
- **调用方式**：优先调用 `data`、`model`、`sets`；先核对参数顺序和返回值。
- **主要输出**：工作区变量或求解器结果
- **调用风险**：未发现明显静态风险，但仍需运行测试和结果校验。

- SHA-256：`49cf2138c60eeda0e45e10cfd333661e776541bff93442389f8986ae92d42b5a`
- 语言：LINGO
- 符号：`data`, `model`, `sets`
- 原始路径：
  - 源文件：`00-Inbox/Downloaded/6.数学建模模型算法大全/司守奎数学建模算法课件/程序及数据/07第7章/ex7_3_2.lg4`

<details>
<summary>展开原始代码</summary>

````text
model:

sets:

variable/1..2/:x;

S_Con_Num/1..4/:g,dplus,dminus;

S_con(S_Con_Num,Variable):c;

endsets

data:

g=1500 0 16 15;

c=200 300 2 -1 4 0 0 5;

enddata

min=dplus(2)+dminus(2);    !二级目标函数;

2*x(1)+2*x(2)<12;

@for(S_Con_Num(i):@sum(Variable(j):c(i,j)*x(j))+dminus(i)-dplus(i)=g(i));

dminus(1)=0;!一级目标约束;

@for(variable:@gin(x));

end
````

</details>
